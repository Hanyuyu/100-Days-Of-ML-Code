# 课程修正与验证记录

## 修正范围

本轮在 2026-09-18 根据逐日审阅更新 Day 1～54 的代码和文字教学材料。图片未修改，Day 55～100 不在本轮新增范围。[原始审阅](day-by-day-review.md)描述的是修正前状态；[原始学习日志](original-roadmap.md)仅为历史参考。

- 8 个原有基础实现课修复旧 API、路径、数据预处理泄漏和脚本/Notebook 差异，补评价指标、参数解释和练习。
- 新增 Day 14 SVM 间隔、15 朴素贝叶斯、16 核 SVM、18 NumPy 神经网络、21 HTML 提取、44 K-means、54 层次聚类的完整实现。
- Day 39～42 更新为稳定 TensorFlow 2/Keras 3 用法，补 Python 和 Markdown，采用本地 MNIST、按批次图像解码、固定划分清单、`.keras` 保存/加载及 TensorBoard 受控实验。
- 其余 35 个理论/阅读日新增本地 Markdown：学习目标、核心说明、可运行最小示例、结果解读和练习。
- 重建完整课程目录、安装文档、数据说明、FAQ 和外部教材兼容说明。历史外链不再作为主要课程入口。
- 19 个实现课由带单元标记的 Python 源同步生成 Markdown/Notebook，减少三份材料长期漂移；生成的 Notebook 不保留过期执行输出。
- 增加基础/深度学习依赖文件、顺序执行检查、关键统计不变量测试，以及 GitHub Actions 配置。

## 结果口径变化

Day 2 统一使用 CSV 全部 28 行。Day 3 不再把零支出填补为均值。分类课采用分层划分，KNN/决策树选参只使用训练内交叉验证，SVM 只用训练集缩放参数。随机森林增加树数并限制最小叶样本。因此新指标不应与旧截图逐值比较。

独热编码、缩放、模型训练仍逐步展示；共享工具只处理路径、报告/绘图和图像数据加载。CNN 结构直接写在课程中，便于学习参数量和层的作用。

## 验证方法与限制

全新环境实测通过：15 个基础脚本 + 15 个基础 Notebook、4 个深度学习脚本 + 4 个深度学习 Notebook、35 个理论日示例、10 项回归测试、同步检查及活动本地链接检查。

运行命令、依赖版本和环境变量见[安装说明](setup.md)。

- 基础检查执行 15 个脚本及 15 个 Notebook，交替从仓库根目录和 Code 启动，验证相对位置处理。
- 深度学习检查执行 4 个脚本及 4 个 Notebook。MNIST 使用本地真实数据子集；猫狗流程使用临时生成的合成图片，检查划分、解码、训练、日志和保存加载，不用其准确率评价真实任务。
- 35 个理论日示例作为独立 Python 程序执行；活动 Markdown 的本地文件链接检查存在性。
- 回归测试验证训练集填补参数、未知类别、零支出、预测不改变缩放参数、NumPy 网络数值梯度，以及图像划分的确定性、重复内容隔离、坏图统计和冲突标签报错。
- 本地验证使用全新虚拟环境（无 system-site-packages），Linux / Python 3.12；基础依赖严格按 requirements.txt 安装，深度学习使用 tensorflow-cpu 2.18.1 / Keras 3.15.1。`pip check` 未发现依赖冲突。CI 配置基础 Python 3.11/3.12、深度学习 Python 3.12；未推送前不能声称远程 CI 已通过。
- 真实猫狗数据完整训练尚未执行。其他操作系统、GPU、NumPy 2/pandas 3 等不同版本不在本轮验证承诺内。

运行检查不会更新教学 Notebook 的输出，也不会执行 Kafka 或个人实验脚本。模型、临时数据和图形不提交到 Git。

## 第二轮复查

本轮重新逐项核对实现课源代码、生成文件、35 个理论日说明、辅助函数、安装文档和检查流程，并修正以下问题：

- TensorFlow 配置只重置种子，不在运行时初始化后重新设置全局线程数，支持 Notebook 重跑；批次管线保留线程限制。
- 猫狗训练先对全部轻量索引打乱再解码，避免原有 512 张图像缓冲区在大数据集上造成前段批次类别偏置；明确非方形图像的高宽顺序与双线性缩放。
- 加载清单时校验所选文件哈希、重复项和每个划分的类别，数据变化后停止评价；明确抽样上限意味着只审查已访问文件，字节哈希不能检测不同编码的相似图片。
- 决策边界和散点共用离散类别颜色映射，支持非 0/1 数值标签；Day 14 实际绘制支持向量、得分 0/±1 等高线并输出间隔宽度。
- Day 18 检查目标数组形状，避免广播错误，记录最后一次参数更新后的真实损失；Day 42 用实际保存的最佳检查点重新评估，保证选择指标与加载模型对应。
- 修正 R² 的评价均值基准、SVM 间隔违例、特征值为负/零、树图深度等文字。补充 CNN 形状、朴素贝叶斯后验比例、轮廓系数、图像管线步骤和类别指标解释。
- Day 51～53 将示例图保存为 PNG，检查在临时目录验证图像产物；每个实现课新增 Python/Notebook/目录导航，配置中的相对路径统一锚定仓库根目录。

新增回归覆盖大样本首批次类别混合、相同种子的可复现性、非方形图像、TensorFlow 初始化后重复配置、文件变更和划分重复，以及类别颜色一致性。全部 10 项回归测试通过。

此外，完整 MNIST 示例在当前固定环境训练 5 轮（48000 训练、12000 验证、10000 测试），最佳验证权重恢复后测试 accuracy 约 **0.9709**、交叉熵约 **0.09164**，保存/加载预测一致。这个数值是一次可复现实验记录，不作为逐位相同的验收阈值。猫狗仍限于合成数据管线验证，未报告真实猫狗精度。
